@{
    Layout = "_HomeLayout";
}

@section AngularBootstrap {
    <script>
            document.addEventListener('WebComponentsReady', function () {
                System.import('/app/admins/boot.js').catch(function (err) { console.error(err); });
            });
    </script>
}
<ic-application userName="@ViewBag.UserName" _lang="@ViewBag.CurrentLanguage">

    @await Html.PartialAsync("_HomePreloadPage")

</ic-application>
